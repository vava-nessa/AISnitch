# Version bump to 0.2.9

## Description

Update the package version across the monorepo, align the `AISNITCH_VERSION` constant, adjust tests and changelog. Ensure no functional changes.

## Acceptance Criteria
- `package.json` and `packages/client/package.json` version set to `0.2.9`.
- `src/package-info.ts` `AISNITCH_VERSION` constant updated.
- All tests expecting version updated to `0.2.9`.
- Changelog entry added for `0.2.9`.
- All tests pass.
